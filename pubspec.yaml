name: college_scoreboard
description: "A new flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  simple_animations: ^5.0.2
  cupertino_icons: ^1.0.8
  http: ^1.2.1
  intl: ^0.19.0
  path_provider: ^2.1.3
  open_file_plus: ^3.4.1
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/vp_logo.png

